<%# locals: (title:, back_path:) %>
<%= render "shared/back_link", back_path: back_path, mb_class: "mb-2" %>
<div class="max-w-4xl mx-auto mb-4">
  <h1 class="text-2xl font-bold"><%= title %></h1>
</div>
