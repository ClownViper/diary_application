<div class="max-w-4xl mx-auto py-8 mb-10">

  <%= render "shared/page_header", title: "日記一覧", button_label: "新規作成", button_path: new_diary_path(date: @date) %>

  <!-- 検索フォーム -->
  <div class="mb-6 bg-white p-4 rounded-xl shadow">
    <%= form_with url: diaries_path, method: :get, local: true, class: "grid grid-cols-1 md:grid-cols-3 gap-4" do |f| %>

      <div>
        <label class="<%= ApplicationHelper::SEARCH_LABEL_CLASS %>">キーワード</label>
        <%= f.text_field :q, value: params[:q],
              placeholder: "タイトル・本文を検索",
              class: ApplicationHelper::INPUT_CLASS %>
      </div>

      <div>
        <label class="<%= ApplicationHelper::SEARCH_LABEL_CLASS %>">日付</label>
        <%= f.date_field :date,
              placeholder: " ",
              class: [ApplicationHelper::INPUT_CLASS, "h-[42px] bg-white text-gray-800 appearance-none"].join(" ") %>
      </div>

      <div class="flex items-end">
        <%= f.submit "検索", class: [ui_button_classes(variant: :primary, size: :md), "w-full"].join(" ") %>
      </div>

    <% end %>
  </div>

  <!-- 一覧カード -->
  <div class="w-full grid grid-cols-1 md:grid-cols-2 gap-6">

    <% @diaries.each do |diary| %>
      <%= link_to diary_path(diary),
            class: "block bg-white rounded-xl border shadow-sm hover:shadow-md transition p-4 flex gap-4" do %>

        <% if diary.image.attached? %>
          <%= image_tag diary.image,
                class: "w-24 h-24 object-cover rounded-lg flex-shrink-0" %>
        <% else %>
          <div class="w-24 h-24 bg-gray-200 rounded-lg flex items-center justify-center text-gray-400 text-sm">
            No Image
          </div>
        <% end %>

        <div class="flex flex-col min-w-0">
          <p class="font-semibold text-gray-800 text-lg truncate">
            <%= diary.title %>
          </p>

          <p class="text-sm text-gray-500">
            <%= diary.date.strftime("%Y/%m/%d") %>
          </p>

          <p class="text-gray-700 text-sm mt-1 line-clamp-2">
            <%= diary.body %>
          </p>
        </div>

      <% end %>
    <% end %>

  </div>

  <% if @diaries.empty? %>
    <div class="bg-white rounded-xl shadow p-6 text-center text-gray-500 mt-4">
      日記はありません。
    </div>
  <% end %>
</div>
